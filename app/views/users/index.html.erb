<% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>
   
   
   
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<div class="container px-5 px-sm-0">  
<div class="row">
    
<div class="col-md-3"><%= render "info", user: current_user, book: @book %></div>

<div class="col-md-8 offset-md-1">
<table class="table">
  <h2>Users</h2>
  <thead>
    <tr>
      <th class="books">image</th>
      <th class="books">name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

 <tbody>
     <% @users.each do |user| %>
   
     <tr>
      
      <td><%= image_tag user.get_profile_image(100,100) %></td>  
       </td>
      <td><%= user.name %></td>
      <td><%= link_to "Show", user_path(user) %></td>

      <td></td>
     
     </tr>
     
    
     
   <% end %>
 </tbody>
</table>

</div>

</div>
</div>
