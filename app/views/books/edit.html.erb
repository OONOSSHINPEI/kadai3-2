<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<main>

<div class="container">  
<div class="row">

<div class="col-sam-12 col-md-8 col-lg-5 px5 px-sm-0 mx-auto">

<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>

<div class="form-group">
  <label for="title">Title</label>
</div>
<div class="form-group">
  <%= f.text_field :title, autofocus: true, id:"inputTitle", placeholder:"Title", class: "form-control" %>
</div>
<div class="form-group">
  <label for="body">Opinion</label>
</div>
<div class="form-group">
  <%= f.text_field :body, autofocus: true, id:"inputOpinion", placeholder:"Opinion", class: "form-control" %>
</div>
<div class="form-group">
  <%= f.submit "Update Book", class: 'btn btn-default btn-success'  %>
</div>
  
<% end %>
<% if @book.user == current_user %>
<%= link_to "Show", book_path(@book) %> | 
<%= link_to "Back", books_path %>
<% else %>
ridirect_to user_path(@user.id)
<% end %>
</div>

</div>
</div>

</main>